- [x] `leaflet` 和 `react-leaflet` 依赖已成功安装，地图底图能够正常加载（基于免费的 OpenStreetMap）。
- [x] 当成功获取当前 GPS 坐标时，地图上会准确出现代表“当前位置”的标记。
- [x] 用户可以通过点击地图任意位置，生成或移动“下一站”的标记。
- [x] “下一站”的坐标已成功存入 LocalStorage，刷新页面后标记依然存在于点击的位置。
- [x] 地图组件在 PC 端和移动端均能正常显示，不会破坏现有的卡片/栅格布局。